{
  "name": "zotion-vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "heroku-postbuild": "npm i -dev  && npm build",
    "start": "vite --port $PORT"
  },
  "devDependencies": {
    "vite": "^3.2.3"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
